version: 2

build:
  os: ubuntu-24.04
  tools:
    nodejs: "24"
  commands:
    - npm install
    - echo $READTHEDOCS_VERSION
    - ./node_modules/.bin/vite build --base $READTHEDOCS_VERSION --outDir $READTHEDOCS_OUTPUT/html